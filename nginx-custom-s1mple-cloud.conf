# Configuración personalizada para s1mple.cloud
# Este archivo NO se debe usar directamente por el error de location duplicado
# Usa el script fix-nginx-hestiacp.sh en su lugar

# Headers adicionales para Next.js (estos sí se pueden agregar)
# Estos headers se añadirán a los ya existentes

# Nota: Para cambiar el proxy_pass, debes modificar directamente nginx.conf
# usando el script: ./fix-nginx-hestiacp.sh

